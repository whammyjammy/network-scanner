version: '3.8'

services:
  vuln-scanner-web:
    build: .
    image: vuln-scanner-web:latest
    container_name: vuln-scanner-web
    network_mode: host
    volumes:
      - ./scans:/output
    ports:
      - "5000:5000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped